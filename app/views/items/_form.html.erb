<%= form_for [user, Item.new] do |f| %>

  <% if Item.new.errors.any? %>
     <div class="alert alert-danger">
       <h4><%= pluralize(Item.new.errors.count, "error") %>:</h4>
       <ul>
         <% Item.new.errors.full_messages.each do |msg| %>
           <li><%= msg %></li>
         <% end %>
       </ul>
     </div>
   <% end %>


   <div class="form-group">
     <%= f.text_area :name, rows: 2, class: 'form-control', placeholder: "enter new to-do item" %>
   </div>
   <div class="form-group">
     <%= f.submit "submit", class: 'btn btn-success' %>
  </div>
<% end %>
